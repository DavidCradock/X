#pragma once
#include "PCH.h"

namespace X
{
	// This is an object which is inserted into the various scene managers
	// It holds an entity's position, rotation and scale within the scene manager.
	// It also holds geometry, the shader name and texture names used to render the entity.
	class SceneManagerEntity
	{
	public:
		SceneManagerEntity();
		
	};
}