#pragma once
#include "PCH.h"

namespace X
{
	// Check for the global GUI "Application state selection" container's button presses and switch states accordingly
	void DemoNNGUIstateSelectionUpdate(CFiniteStateMachine* pFSM);
}